/**
 * Contains Category Elements.
 * Category Elements are custom elements like sub-categories (Config) or top level categories (ModConfig).
 * These elements are not backed by a {@link net.minecraftforge.common.ForgeConfigSpec.ConfigValue}.
 * They are necessary glue for the config system but they are not proper config elements.
 */

package net.minecraftforge.fml.client.config.element.category;
